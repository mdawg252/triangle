name: triangle

usage: triangle [number] [options]

options:
	-f, --fill                              = fill triangle with set character
	-c [character], --character [character] = set character (default *)
